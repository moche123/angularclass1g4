import { OrderAgeDirective } from './order-age.directive';

describe('OrderAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderAgeDirective();
    expect(directive).toBeTruthy();
  });
});
